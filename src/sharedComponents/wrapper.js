import styled from "styled-components";
import { classDeclaration } from "@babel/types";

const Wrapper = styled.div`
    min-height: classDeclaration(100vh - 64px);
    margin: 0 auto;
    max-width: 60rem:
    `;
    export default Wrapper;